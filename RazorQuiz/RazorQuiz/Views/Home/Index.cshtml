@model RazorQuiz.Models.Vehicle

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    @if (Model == null)
    {
        @Html.ActionLink("Car", "Index", new { vehicleType = "car" })
        <br />
        @Html.ActionLink("Boat", "Index", new { vehicleType = "boat" })
        <br />
        @Html.ActionLink("Spaceship", "Index", new { vehicleType = "spaceship" })
        <br />
        
        @*    action links vs html
            
            <div>
            <a href="~/Home?vehicleType=car">Car</a>
        </div>
        <div>
            <a href="~/Home?vehicleType=boat">Boat</a>
        </div>
        <div>
            <a href="~/Home?vehicleType=spaceship">Spaceship</a>
        </div>*@
    }
    else
    {
        <div>
            I @Model.Verb the @Model.Noun
        </div>
    }
</body>
</html>
